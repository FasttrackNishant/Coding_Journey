#include <stdio.h>
int main()
{
    int a;
    printf("hello c iam  back\n");
    int arr[20];
    for (int i = 0; i < 4; i++)
    {
        printf("The value of i is %d", i);
        scanf("%d", &arr[a]);
    }
    for (int i = 0; i < 4; i++)
    {
        printf("The value at %d index is %d \n", i, arr[i]);
    }

    return 0;
}